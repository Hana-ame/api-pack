改成gin了。
除了文件夹和main.go都是之前的剩余。
有需求再改，go不是很想用，转react/nextjs/java去。



> import "api-pack/nyaa-proxy" is a program, not an importable packagego list

package's name should not be `main`



其实应该能写出通用 jarloader的， 懒得写了。先放着吧。
